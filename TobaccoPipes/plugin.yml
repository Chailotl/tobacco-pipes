name: TobaccoPipes
description: Tobacco pipes you can smoke with
author: Raus
main: com.raus.tobaccoPipes.Main
version: 1.0
api-version: 1.15

commands:
  pipes:
    description: Tobacco pipes
    default: true
    usage: "Usage: /pipes [info|reload]"

permissions:
  pipes.reload:
    default: op
    description: Allows you to reload the config